language: cpp

sudo: required

cache: ccache

dist: trusty

os:
  - linux
  - osx

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
    - freeglut3-dev
    - libosmesa-dev
    - libpng-dev
  brew:
    packages:
       - libpng --universal
       

script:
  - cat /etc/apt/sources.list

